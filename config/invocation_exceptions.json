{
  "_purpose": "Artifacts that legitimately have no invoker inside the framework (RA-16 INVOCATION_COVERAGE, agents.md §7). Read by scripts/verify_references.py check (d).",
  "_why_a_data_file": "Not frontmatter: vendored SKILL.md files cannot be edited at all under the Skill Documentation Veto (rules/skills_and_integrations.md §3).",
  "_valid_reasons": ["model-invoked", "vendored-reference", "human-entry-point", "one-time"],
  "_rule": "An entry pointing at a path that no longer exists fails the check. A stale exemption silently excuses the next artifact to take that name.",
  "exceptions": [
    {
      "path": "skills/django-patterns-3rd",
      "reason": "vendored-reference",
      "note": "Vendored from affaan-m/ECC (MIT, see NOTICE.md). Reference material an agent consults when working on a Django host; not a step in any framework workflow."
    },
    {
      "path": "skills/django-security-3rd",
      "reason": "vendored-reference",
      "note": "Vendored from affaan-m/ECC (MIT). Same status as django-patterns-3rd."
    },
    {
      "path": "skills/django-tdd-3rd",
      "reason": "vendored-reference",
      "note": "Vendored from affaan-m/ECC (MIT). Same status as django-patterns-3rd."
    },
    {
      "path": "skills/django-verification-3rd",
      "reason": "vendored-reference",
      "note": "Vendored from affaan-m/ECC (MIT). Same status as django-patterns-3rd."
    },
    {
      "path": "skills/django-expert-3rd",
      "reason": "vendored-reference",
      "note": "Vendored Django reference pack (rules/django_backend_standard.md). Consulted when working on a Django host; not a step in any framework workflow. Ships an accepted vendored-upstream nested skills/ sub-layer (rules/django_backend_standard.md)."
    },
    {
      "path": "skills/autoskills-3rd",
      "reason": "vendored-reference",
      "note": "Local Arsenal Bridge (rules/skills_and_integrations.md §1 Priority 2). Reference material the skill_architect consults; not a scripted workflow step."
    },
    {
      "path": "skills/skills-samples-3rd",
      "reason": "vendored-reference",
      "note": "Sample skills kept as authoring examples for skill_forge_workflow.md. Wiring them into a workflow step would execute examples as if they were tools."
    },
    {
      "path": "skills/skill-creator",
      "reason": "model-invoked",
      "note": "Claude invokes it by name when authoring a skill. skill_forge_workflow.md governs the procedure; this is the instrument Claude reaches for inside it."
    },
    {
      "path": "skills/contract-writer",
      "reason": "model-invoked",
      "note": "Now named by reverse_documentation_workflow.md Phase 6. Listed here because the workflow names it in prose rather than as a scripted call, so the automated scan cannot resolve it as a wired invocation."
    },
    {
      "path": "skills/python-quality-auditor",
      "reason": "model-invoked",
      "note": "Named by agents.md §1's linter_command/max_indentation/max_lines_per_func rows. Corrected at Sprint 049 (F-049-7): its script shells to ruff/mypy/bandit/radon and prints a pass/fail line per tool; it computes no numeric score, never checks indentation or function length, and never exits non-zero. `ruff check .` remains the real deterministic linter for Python. The exception is kept, not withdrawn: this skill stays a model-invoked helper — Sprint 050 delivered the deterministic instrument for function length and nesting depth as `scripts/quality_audit.py` (invoked via `make quality-audit`), not as a replacement for this skill."
    },
    {
      "path": "skills/js-standardizer",
      "reason": "model-invoked",
      "note": "Named by agents.md §1's linter_command/max_indentation/max_lines_per_func rows. Corrected at Sprint 049 (F-049-7): its script checks for lint-tool config file presence and a single repo-wide boolean (any .js/.ts file anywhere contains \"@param\" or \"@returns\"), not per-function JSDoc coverage; it computes no numeric score, never checks indentation or function length, and never exits non-zero. `pnpm run lint` remains the real deterministic linter for JS/TS. The exception is kept, not withdrawn: this skill stays a model-invoked helper. Sprint 050 built a deterministic JS/TS instrument in `scripts/quality_audit.py` and withdrew it (Abort criterion 1) after four consecutive gate rejections found silent-compliance defects a stdlib-only scanner can't fix in general; no deterministic instrument currently exists for JS/TS complexity. Re-planned as its own sprint: `KI-050-6`."
    },
    {
      "path": "skills/mcp-registry",
      "reason": "model-invoked",
      "note": "Used when registering an MCP server (agents.md §5 discovery_manifest). The merge itself is install.sh's job; this skill is for authoring the entry."
    },
    {
      "path": "skills/topology-scaffolder",
      "reason": "model-invoked",
      "note": "The instrument topology_mapper reaches for when scaffolding [Stack]/[Layer] directories. The agent profile owns the decision; the skill performs it."
    }
  ]
}
