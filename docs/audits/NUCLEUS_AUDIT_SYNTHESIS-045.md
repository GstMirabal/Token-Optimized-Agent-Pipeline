# 🏛️ Structural Audit Report: Nucleus Ruleset / Workflow / Mechanism Audit — Wave 2 Synthesis

**Audit ID**: #A045-SYNTH
**Auditor**: rule_validator
**Date**: 2026-09-07
**Sprint**: 045 (nucleus-ruleset-mechanism-audit) · Phase 6 · Unit 4 (Wave 2 — synthesis)
**Mode**: NUCLEUS · Branch `ai-sprint/045`
**Scope**: consolidation of Units 1–3 — `NUCLEUS_RULESET_AUDIT_REPORT-045.md`, `NUCLEUS_WORKFLOW_AUDIT_REPORT-045.md`, `NUCLEUS_MECHANISM_AUDIT_REPORT-045.md`
**Nature**: classification + drafted amendments only. This report **applies nothing** (`IMPLEMENTATION_PLAN.md` decision D1). No `agents.md`, `rules/*.md`, `workflows/*.md`, script or hook was edited. Every "Proposed amendment" cell below is drafted text, not an applied change.

---

## 🚦 Executive Summary

Wave 2 merges the three Wave-1 unit reports into one impact-ranked register, cross-checks it against the open upstream-findings ledger, and buckets every actionable item into a recommended execution sprint. Units 1–3 classified 129 normative units and mechanisms; 2 are `OBSOLETA` (both in the always-loaded constitution) and 23 are `MEJORAR`. The synthesis register carries **29 actionable rows** (`S045-01`..`S045-29`) plus **1 RECORD row** (`S045-30`, no action). All 29 route `nucleus` — this is the nucleus auditing its own corpus. Zero rows duplicate an open upstream finding, because **0 upstream findings are currently open** (`docs/audits/UPSTREAM_FINDINGS_FROM_HOSTS.md`, latest status table — Sprint 033, `ai-sprint/033`: "Still open: *(none in this file's open set)*").

| Metric | Value | Status |
| :--- | :--- | :--- |
| Units consolidated | 3 (ruleset, workflows, mechanisms) | ✅ |
| Norms / mechanisms audited (all units) | 129 | ✅ |
| `OBSOLETA` (all units) | 2 | ⚠️ |
| `MEJORAR` (all units) | 23 | ⚠️ |
| Synthesis register — actionable rows | 29 (`S045-01`..`S045-29`) | ⚠️ |
| Synthesis register — RECORD rows (no action) | 1 (`S045-30`) | ℹ️ |
| Rows routed `nucleus` | 29 / 29 actionable | ✅ |
| Open upstream findings | 0 | ✅ |
| Rows duplicating an open upstream finding | 0 | ✅ |

---

## 🔍 1. Consolidated Ranked Register

**Ranking formula.** Tier by corpus reach: always-loaded `agents.md` (T1) > on-trigger `rules/` (T2) > `workflows/` (T3) > `scripts`/`hooks` (T4). Within a tier, a correctness / contradiction defect outranks a precision / cosmetic one. `ID` is sequential in final rank order.

`routing_class` per `agents.md §4 feedback_upstream` — one of `nucleus` | `profile` | `host` | `discard`. `Recommended execution sprint`: `046` = fast, low-risk (cosmetic + broken-reference + doc fixes); `047` = needs a design pass (enforcement wiring, cross-file text moves, `RA-14`-bound step-id renumbering, script enhancements); `defer` = needs a second occurrence or a human policy call.

| ID | Source unit | Norm / Mechanism | Verdict | Evidence (1 line) | Proposed amendment (drafted, NOT applied) | routing_class | Duplicate of upstream? | Rec. sprint |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| S045-01 | Unit 1 | `agents.md §7 RA-04: FULL_DEPLOYMENT` | OBSOLETA | `grep -rn 'RA-04' .` → single hit `agents.md:153`; 0 citations in 44 sprints; "Orchestrator MUST deploy the COMPLETE task hierarchy" contradicts `§6 orchestrator` (roadmap author, not deployer) + `RA-12` + `deployment_workflow.md` Phase 1 (CC-9). | Replace row body with a tombstone: "Retired Sprint 045 — superseded by `§6 orchestrator` + `RA-12`." **Do NOT renumber `RA-05`..`RA-18`.** If the scaffolding-atomicity intent survives, fold one sentence into the `§6 orchestrator` charter citing `skills/sprint-architect/`. | nucleus | n/a (0 open) | 046 |
| S045-02 | Unit 1 | `agents.md §1 linter_command` + `Complexity` (CC-10) | MEJORAR | `grep -n 'ruff check\|python-doctor\|react-doctor\|pnpm run lint' Makefile` → 0 hits; `make verify` (`Makefile:51-92`) runs no linter, `*-doctor`, `max_lines_per_func` or `max_indentation` check; `python-doctor`/`react-doctor` binaries exist nowhere on disk; `memory_index.json:94` confirms. | Add a `Verified by:` pointer on both rows to the QA-gate skill that measures them (`python-quality-auditor` / `js-standardizer`, `config/invocation_exceptions.json:43,48`); state plainly they are **not** part of `make verify` (deterministic vs QA-judgment split, `code_craft.md` style). Rename or drop the `python-doctor` / `react-doctor` tool names. | nucleus | n/a (0 open) | 047 |
| S045-03 | Unit 1 | `agents.md §3 federation` + `§5 discovery_manifest` (CC-4) | MEJORAR | `federation` (`agents.md:77`) + `discovery_manifest` (`agents.md:111`) name `install.sh` as the *exclusive* bridge; `scripts/install.sh:1-6` is a 6-line `exec python3 install.py "$@"`; `README.md:60` + `AGENTS_SLASH_COMMANDS_GUIDE.md:16,35,138` call `install.sh` a "deprecation shim" and `install.py` the sanctioned bridge. | Replace `install.sh` → `install.py` in both rows: "the sanctioned bridge is `.agents/scripts/install.py` (`install.sh` is a deprecation shim that forwards to it)". | nucleus | n/a (0 open) | 046 |
| S045-04 | Unit 1 | `agents.md §3 enforcement` (CC-5) | MEJORAR | Row names `mass-standardizer` as "the official auditor for this standard"; `audit_workflow.md:17` + `Makefile:69` run `skills/topology-monitor/scripts/legacy_app_auditor.py` for `skill_standard_check`; `mass-standardizer` runs only `generate_manifest.py` (`Makefile:70`). | "the official auditor for adherence is `skills/topology-monitor/scripts/legacy_app_auditor.py`; `mass-standardizer` regenerates `skills/manifest_skills.json`." | nucleus | n/a (0 open) | 046 |
| S045-05 | Unit 1 + Unit 3 (F-3) | `agents.md §2 ast_skeleton` / `graph_sovereignty` / `graph_sync` (CC-6, CC-7) | MEJORAR | `ast_skeleton` cites `omni_minimizer.py` bare (real path `skills/omni-context-minimizer/scripts/omni_minimizer.py`); `graph_sovereignty` cites `graph.json` — root file absent (`Glob graph*.json` → only `graphify-out/graph.json`); `graph_sync` mandates bare `graphify update`, which `rules/graphify.md:9-10` records as failing `bad interpreter`. Same bare `omni_minimizer.py` at `rules/token_economy.md:11` (`RA-14` grep-set). | `ast_skeleton`: "invoke `skills/omni-context-minimizer/scripts/omni_minimizer.py` (skill: `omni-context-minimizer`)". `graph_sovereignty`: "Query `graphify-out/graph.json`". `graph_sync`: "running `venv_skillopt/bin/python -m graphify update .`". Same patch fixes `rules/token_economy.md:11` and `workflows/pipeline_workflow.md:37` (`RA-14`). | nucleus | n/a (0 open) | 046 |
| S045-06 | Unit 1 | `agents.md §7 RA-01: ENVIRONMENT_VIOLATION` | MEJORAR | Only substantive definition is `agents.md:150`; every other hit is a range expression; 0 standalone citations in `workflows/`, `rules/`, guides. Body "Agent MUST attempt autonomous remediation EXCEPT if prohibited" names no prohibiting authority, no operation, no done-criterion — a `§1 unambiguous_action` breach inside the constitution. | Rewrite: "On an environment/tooling failure (missing venv, absent binary, broken lockfile) the agent MUST attempt one scoped autonomous fix through the isolated local environment (`§3 dependencies`) before escalating, UNLESS `rules/project_topology.md` or the Implementation Plan names that remediation human-only. Procedure: `workflows/remediation_workflow.md` for loop stalls; `RA-03` for CRITICAL/HIGH defects." Add an `invoked_by`-style pointer. | nucleus | n/a (0 open) | 047 |
| S045-07 | Unit 1 | `agents.md §7 RA-10: SUPPLY_CHAIN_SHIELD` (CC-1) | OBSOLETA | `agents.md:159` is a strict **subset** of `§8` (`agents.md:173-176`), which repeats `pnpm 11+` / `ignore-scripts=true` / `minimum-release-age=1440` and adds `onlyBuiltDependencies`. Two always-loaded copies; `§8` is the superset. 3 external citations. | Degrade the row to a pointer: "`RA-10: SUPPLY_CHAIN_SHIELD` — see `§8 Supply Chain Security` (canonical)." **Do NOT renumber.** Keeps the 3 external `RA-10` citations resolvable. | nucleus | n/a (0 open) | 046 |
| S045-08 | Unit 1 | `agents.md §0 Certification` ≡ `RA-05`; `§3 secret_sovereignty` ≡ `RA-09` (CC-2, CC-3) | MEJORAR | `§0 Certification` (`agents.md:16`) is near-verbatim of `RA-05` (`:154`); `§3 secret_sovereignty` (`:86`) duplicates `RA-09` (`:158`); `LEGACY_RULE_CONCORDANCE.md:18` names both `§3 secret_sovereignty` **and** `RA-09` as authority for `Rule 66`. | Keep `RA-05` and `RA-09` as the single §7 definitions (each adds mechanism the section lacks); degrade `§0 Certification` to "See `RA-05: SPRINT_CLOSEOUT`" and `§3 secret_sovereignty` to "See `RA-09: SECRET_SOVEREIGNTY`." Update `LEGACY_RULE_CONCORDANCE.md:18` to a single authority. | nucleus | n/a (0 open) | 046 |
| S045-09 | Unit 1 | `agents.md §7 RA-02: LAZY_SIGNAL_PARADIGM` (CC-8) | MEJORAR | Circular: `rules/django_backend_standard.md:55-56` calls `agents.md §7` "the single definition" while `§7` is a one-line summary — a reader loading only one file gets a stub. Django-only rule sitting in the always-loaded constitution. | Move the operative text ("local imports inside the receiver; lazy sender strings") into `rules/django_backend_standard.md §2` as the definition; degrade the `agents.md §7 RA-02` row to "See `rules/django_backend_standard.md §2 Signal Registration`." Keep the `RA-02` anchor resolvable for `Clause J-02` via `LEGACY_RULE_CONCORDANCE.md`. `RA-14` grep-set: both files. | nucleus | n/a (0 open) | 047 |
| S045-10 | Unit 1 | `agents.md §7` section — `RA-16` print-order (CC-11) | MEJORAR | `RA-16` printed at `agents.md:164`, before `RA-15` at `:165` (`grep -n 'RA-1[56]' agents.md`); breaks monotonic `RA-NN` lookup. `README_TEMPLATE.md:77` still describes §7 as "`RA-01`…`RA-15`". | Swap the two rows so the table reads `… RA-14, RA-15, RA-16, RA-17, RA-18`. Update `docs/standards/templates/README_TEMPLATE.md:77` range to `RA-01…RA-18`. No text change to either amendment. | nucleus | n/a (0 open) | 046 |
| S045-11 | Unit 1 | `rules/frontend_modular_standard.md` | MEJORAR | H1 is `# 🛡️ Rule 041: Frontend Modular Standard` (`:1`); subsections `(Rule 41.1)` / `(Rule 41.2)` / `(Rule 41.3)`; footer `*Status: ACTIVE*` is not the `documentation_standard.md §4.1` metadata block. `LEGACY_RULE_CONCORDANCE.md:27` freezes numbered citations to that table; `RA-06` mandates keyed naming. Sibling `rules/django_backend_standard.md:17-22` says the two files are "named to match" — they do not. | Retitle to `# Rule Context: Frontend Modular Standard`; rename subsections to `## 1. Directory Anatomy` etc. (drop `(Rule 41.x)`); add the intro sentence + trigger line the other `rules/` files carry; replace the footer with the `§4.1` metadata block. Keep the `Rule 41 / 041 / 41.x` row at `LEGACY_RULE_CONCORDANCE.md:15`. | nucleus | n/a (0 open) | 046 |
| S045-12 | Unit 1 | `rules/project_topology.md:6` | MEJORAR | §1 mandates `./venv/bin/python` and `./node_modules/.bin/npx` as interpreter examples; the framework's own interpreter is `venv_skillopt/bin/python` (`Makefile:23`, `rules/graphify.md:10`); a nucleus session applying §1 literally looks for a non-existent `./venv/`. `./.docker-db-data`, `./data/output/` are host conventions with no nucleus referent. | Add: "Nucleus interpreter is `venv_skillopt/bin/python` (`Makefile`); the `./venv/` form is the host-project convention." Mark the DB/ETL sections `host-only`. | nucleus | n/a (0 open) | 046 |
| S045-13 | Unit 2 | `workflows/remediation_workflow.md:4` frontmatter `invoked_by:` | MEJORAR | `invoked_by: … rules/qa_and_testing.md#functional_lock` — the `#functional_lock` anchor does not exist (`Grep functional_lock rules/qa_and_testing.md` → no matches). Passed `make verify` because `verify_references.py` check (d) does not resolve `#anchor` fragments in `invoked_by:`. | Repoint to `rules/qa_and_testing.md §4` (RA-17 verdict classes — the real trigger: 3rd consecutive `REJECTED`) and `pipeline_workflow.md#7-quality-gate`. Drafted text: Unit 2 amendment A1. | nucleus | n/a (0 open) | 046 |
| S045-14 | Unit 2 | `workflows/audit_workflow.md` — `precision_audit` / `nomenclature` / `report` / path parity | MEJORAR | `precision_audit` (`:20`) cites `train_runner.py` "dry-run mode" — no such mode; the sole `train_runner.py` (`skills/skillopt/scripts/train_runner.py`) needs `--skill` + `--config`. `nomenclature` (`:19`) cites the decommissioned `[Stack]/[Layer]/[Sprint_ID]` topology + a destructive "forcibly auto-corrects" verb with no gate. `report` (`:22`) names `pipeline_audit_report.md` — not RA-06 Option B (live convention `docs/audits/PIPELINE_AUDIT_REPORT-042.md`). `skill_standard_check` (`:17`) uses `.agents/skills/…` host prefix while sibling `link_audit` uses bare `skills/…`. | Apply drafted Unit 2 amendments A2 (real `train_runner.py` invocation + skip-if-absent + written-diff done-criterion) and A3 (canonical `docs/sprints/[Sprint_ID]-[Stack]-[Layer]/`; "lists … as a rename candidate for human approval"; `docs/audits/PIPELINE_AUDIT_REPORT-[Sprint_ID].md`; both path forms per step). | nucleus | n/a (0 open) | 047 |
| S045-15 | Unit 2 | `workflows/standardization_workflow.md:50` Phase 6 end-state list | MEJORAR | End-state list names only `docs/0_SYSTEM_OVERVIEW.md`; omits `docs/0_SYSTEM_ARCHITECTURE.md`, which `agents.md §0`, `close_workflow.md:20 history_sync` and `rules/documentation_standard.md:22 §2` all treat as a scaffolded sibling anchor (C4 Level 2). `Grep 0_SYSTEM_ARCHITECTURE` → present in close/documentation/12 PHASE_REGISTERs, never in `standardization_workflow.md`. Consumed but never produced. | Add `docs/0_SYSTEM_ARCHITECTURE.md` (from its template) to the Phase 6 end-state list. Drafted text: Unit 2 amendment A4. | nucleus | n/a (0 open) | 046 |
| S045-16 | Unit 2 | `workflows/skill_forge_workflow.md` — `skillopt_run` (`:19`) + `forge_destination` (`:16`) | MEJORAR | `skillopt_run` cites bare `train_runner.py` (resolves only at `skills/skillopt/scripts/train_runner.py`; `close_workflow.md:17` already spells the full path). `forge_destination` option (b) names `.agents/profiles/[name]/skills/` only, omitting the real-project host-controlled route `scripts/install.py --profile-path <path>` (`agents.md §3 topological_order`, `RA-15`, Sprint 028 — production profiles never committed to the public nucleus). | Full path + exit-0 done-criterion for `skillopt_run`; add the `--profile-path` real-project route to `forge_destination` option (b), keeping `profiles/[name]/` for illustrative packs only. Drafted text: Unit 2 amendment A5. | nucleus | n/a (0 open) | 046 |
| S045-17 | Unit 2 (duplication) | SkillOpt `train_runner.py` cited 3 ways in 3 workflows | MEJORAR | `audit_workflow.md precision_audit` (bare name, "dry-run mode"), `close_workflow.md rules_optimization` (full path, "if failures occur"), `skill_forge_workflow.md skillopt_run` (bare name) — no shared definition, each carries its own "(requires explicit authorization)". | Add one canonical invocation block (path, args, config, done-criterion, authorization gate) in `rules/skills_and_integrations.md` or `rules/token_economy.md`; the three workflow steps reference it — not restate it (`RA-14` spirit). | nucleus | n/a (0 open) | 047 |
| S045-18 | Unit 2 | `workflows/reverse_documentation_workflow.md` | MEJORAR | Phases 2,3,4.5,5,6,6.5,6.7,7,8,9,9.5 are `**Bold sentence.**` prose — 11 of ~13 steps `?` in `map_workflows.py`. Fractional numbering is deliberately frozen (`RA-14`, in-file line 51). Frontmatter invoker `audit_workflow.md#findings-handoff` resolves only by intent — Phase 9.5 is titled "Findings handoff", not slugged. | Add a parenthetical step id per row without changing phase numbers (`Phase 5 (correct_false)`, `Phase 6 (write_contracts)` …). Add `<a id="findings-handoff"></a>` at Phase 9.5 (or align the frontmatter slug). Drafted text: Unit 2 amendment A7. | nucleus | n/a (0 open) | 047 |
| S045-19 | Unit 2 | `workflows/repository_hardening_workflow.md` | MEJORAR | All 8 Execution-Flow rows (`:22-31`) are prose sentences — no step ids, `?` for every one; the `gh api` verify calls live only in the phase prose. `RA-16` precedent: "`/agents:harden` shipped in PR #29 and was never run against this repository — five platform controls sat disabled for weeks". No nucleus/host applicability line. | Re-shape the table to `| Phase | Step id | Operation | Verify |`, lifting the `gh api` verify calls from the prose. Add a "Mode" line stating nucleus applicability. Drafted text: Unit 2 amendment A6. | nucleus | n/a (0 open) | 047 |
| S045-20 | Unit 2 | `workflows/standardization_workflow.md` — `condition_check` (`:15`) + bare `/docs/` `/backend/` paths (`:18,35-46`) | MEJORAR | `condition_check` is passive ("Triggered if naming drift … are detected") — no actor, no detection tool. Phase 3 / Legacy Routing Table use bare `/docs/` and `/backend/`; `/backend/` is host-only with no nucleus note; several routing-table rows are misparsed as steps by `map_workflows.py` (the `+ 7 table-row artifacts`). | State who runs detection and with which script (`legacy_app_auditor.py` / `noise_purge`). Mark `/backend/` host-only. Tag or relocate the routing table so `map_workflows.py` skips it (couples with S045-27). | nucleus | n/a (0 open) | 047 |
| S045-21 | Unit 3 (F-7) | `hooks/state_mirror.py:27-28` | MEJORAR | `except json.JSONDecodeError: pass` with an explanatory comment but **no logging** — `agents.md §1 exception_handling` is categorical: "No `pass` in except. Explicit logging required." No blocking path (silent shadow copy on the Stop hook). | Replace the bare `pass` with `print("[state_mirror] active_state.json is not valid JSON; mirror skipped", file=sys.stderr)`. One stderr line satisfies §1 without noise on the happy path. Drafted text: Unit 3 amendment A7. | nucleus | n/a (0 open) | 046 |
| S045-22 | Unit 2 (meta-finding 1) | `scripts/verify_references.py` check (d) — `invoked_by:` sub-anchor gap | MEJORAR | Check (d) confirms a workflow *has* an invoker and that filename mentions resolve, but does not resolve `#anchor` fragments inside `invoked_by:` — which is why `remediation_workflow.md#functional_lock` (S045-13) passed the gate. | Extend check (d) to resolve `file.md#anchor` against the target file's headings / step ids; fail on an unresolvable fragment, same as an unresolvable filename. | nucleus | n/a (0 open) | 047 |
| S045-23 | Unit 3 (F-6) | `scripts/check_gate_log.py:17` docstring | MEJORAR | Exit-code docstring line 17 attributes the blocking `exit 2` to `(RA-11)`; the condition detected is an **RA-17** vocabulary/class mismatch. The verdict vocabulary itself is an exact match to `rules/qa_and_testing.md §4` (`:31-33,89-94`) — no behavioural drift, doc mis-citation only. | Docstring `:17` → `2 — vocabulary or class mismatch (RA-17; exit-code-2 blocking per RA-11 semantics)`. Drafted text: Unit 3 amendment A5. | nucleus | n/a (0 open) | 046 |
| S045-24 | Unit 3 (F-4) | `hooks/on_commit.py:12-14` docstring | MEJORAR | "Exit codes: 0 — allowed / 1 — rejected" predates the Claude Code `PreToolUse` dual-wiring (`claude/settings.hooks.json:69-79`). Code is correct — `block()` → `sys.exit(2)` (`:824-828`), no `exit(1)` anywhere — but the stale docstring invites a future "simplify" back to `exit(1)`, an `RA-11` regression. | Rewrite the "Exit codes" block: `2 — rejected (blocks the git commit AND the Claude Code PreToolUse Bash call; RA-11 requires 2, not 1)`. Drafted text: Unit 3 amendment A6. | nucleus | n/a (0 open) | 046 |
| S045-25 | Unit 3 (F-1) | `scripts/merge_json.py`, `scripts/cursor_adapter.py` | MEJORAR | Both pass `RA-16` check (d) only via AST **import** resolution by `scripts/install.py`; neither carries the literal `invoked_by:` token the other 37 scripts and 5 real hooks carry. Coverage is real, declaration is implicit. | `merge_json.py` docstring: `invoked_by: scripts/install.py (import; non-destructive settings/mcp merge).` `cursor_adapter.py` docstring: `invoked_by: scripts/install.py (import; --target cursor\|both).` Drafted text: Unit 3 amendment A4. | nucleus | n/a (0 open) | 046 |
| S045-26 | Unit 3 (F-2) | `config/invocation_exceptions.json` — `skills/autoskills-3rd`, `skills/django-expert-3rd` | MEJORAR | Both satisfy check (d) branch `skill.name in governance` purely because the directory name appears in a rule sentence; their four `django-*-3rd` siblings carry explicit `vendored-reference` exceptions. Asymmetric and fragile — reword the rule sentence and the skill silently loses coverage. All 11 current exception paths exist (0 stale). | Add two `exceptions[]` entries with `reason: "vendored-reference"` and a `note` (Local Arsenal Bridge / vendored-upstream shape). Drafted text: Unit 3 amendments A3 / Table 1. | nucleus | n/a (0 open) | 046 |
| S045-27 | Unit 2 (meta-finding 2) | `scripts/map_workflows.py` step-verb heuristic | MEJORAR | 49 `?` classifications conflate three distinct causes: genuine ambiguous verbs, prose-sentence rows with real content, and non-step table rows misparsed as steps. A downstream reader of `WORKFLOWS_STEP_MAP_GUIDE.md` cannot tell them apart. | Add a second column or a skip-marker so non-step tables are excluded from step parsing; distinguish "ambiguous verb" from "prose step" from "not a step". Couples with S045-18 / S045-20. | nucleus | n/a (0 open) | 047 |
| S045-28 | Unit 3 (F-8) | `scripts/audit_cursor_era.py` docstring | MEJORAR | Sprint range frozen 026–033 (`:28-29`); output `docs/audits/CURSOR_ERA_EXECUTION_AUDIT.md` is a one-shot census, deliberately not in `verify`. Standing `make cursor-era-audit` target invokes it (`Makefile:119-120`) — refutes any "OBSOLETA" read, but a future auditor could read the frozen range as rot. | Add one docstring line: "Scope frozen to sprints 026-033 (Cursor era). Not stale — a bounded historical census, re-runnable but not expected to change." Drafted text: Unit 3 amendment A8. | nucleus | n/a (0 open) | 046 |
| S045-29 | Unit 2 (meta-finding 3) | `docs/active_state.json` — never-executed-protocol visibility | MEJORAR | `repository_hardening_workflow.md` has no mechanism to record whether it has ever run against a given repository; `start_workflow.md platform_probe` only *proposes* it. `RA-16`'s own precedent is this workflow. | Add a `last_harden_run` field alongside `last_platform_probe`; surface a never-run state in `start_workflow.md platform_probe`. **Needs a human policy call**: does the platform-hardening protocol apply to the nucleus itself, or only to hosts? | nucleus | n/a (0 open) | defer |
| S045-30 | Unit 3 (F-5) | `hooks/on_commit_msg.py:73`, `hooks/on_push.py:55` | RECORD (not a defect) | Both use `return 1` → `sys.exit(main())`. These are **native git hooks** (`commit-msg`, `pre-push`), not Claude Code `PreToolUse` hooks; `RA-11` is explicitly scoped to Claude Code `PreToolUse`. For a git hook any non-zero exit aborts, so `exit 1` is correct. | None. Optionally add one docstring line to each: "Native git hook — any non-zero aborts; RA-11 (Claude Code PreToolUse exit-2 rule) does not apply here." Recorded so a future audit does not misfile this as an `RA-11` breach (`RA-17` `RECORD` / `testifying` class). | discard | n/a (0 open) | n/a |

---

## 🔗 2. Cross-Check Against Open Upstream Findings

Source: `docs/audits/UPSTREAM_FINDINGS_FROM_HOSTS.md` (1354 lines — not dumped; status markers grepped).

- `Grep` of `Still open` / `Opened, still open` markers returns eight per-sprint status tables. The **latest** is *Status at Sprint 033 (2026-08-25, `ai-sprint/033`)*: **"Still open: *(none in this file's open set)*"** — `F-021-A2` (the last carried item) was closed there via the auxiliary `implementer-agent` / ADR-0009 transfer. Earlier still-open items (`F-023-S4`, `F-026-A1/A2/A3`, `F-093-G1`) each show a later "Closed" row (Hotfix H-002, Sprint 027, Sprint 030, Sprint 031 respectively).
- **0 upstream findings are currently open.** Stated once here; the `Duplicate of upstream?` column is `n/a` for every register row.
- No register row duplicates even a *closed* finding: the two nearest neighbours — `F-023-S4` (RA-09 dotenv detection in `on_commit`, closed H-002) and `F-093-G1` (RA-17 verdict classes, closed Sprint 031) — concern mechanism behaviour, whereas `S045-08` (RA-09 doc duplication) and `S045-23` (`check_gate_log.py` docstring mis-citation) are documentation defects in different artifacts.

---

## 📊 3. Consolidated Verdict Counts

Figures as stated in each unit's Executive Summary.

| Unit | Norms / mechanisms audited | VIGENTE | MEJORAR | OBSOLETA |
| :--- | :--- | :--- | :--- | :--- |
| Unit 1 — ruleset (`agents.md` §0–§8 + `RA-01`..`RA-18` + 11 `rules/*.md`) | 38 | 27 | 9 | 2 |
| Unit 2 — workflows (`workflows/*.md`) | 12 | 6 | 6 | 0 |
| Unit 3 — mechanisms (39 scripts + 6 hooks + 34 skills) | 79 | 75 | 8 | 0 |
| **Total** | **129** | **108** | **23** | **2** |

**Reconciliation notes (source-internal slack, not a synthesis error):**
- Unit 1's Executive Summary tallies `MEJORAR = 9`; its Classification Register enumerates ~14 granular `MEJORAR` sub-findings (6 `agents.md` sections + 5 amendments + 3 rule files), several of which are two halves of one cross-cutting finding (e.g. `§0 Certification` ≡ `RA-05`). This synthesis lists the granular items as distinct register rows because each carries a distinct fix.
- Unit 3's Executive Summary counts `MEJORAR = 6 (4 scripts, 2 hooks) + 2 skill annotations = 8`; its `VIGENTE` figure ("41 scripts/hooks + 34 skills") carries a small arithmetic overlap (`merge_json.py` / `cursor_adapter.py` counted once as VIGENTE-by-import and once flagged `MEJORAR` for the missing token). Treated as 8 `MEJORAR` above.
- Consequently the `Total` row sums the per-unit summary figures; it is indicative, not a headcount of the 29 synthesis register rows.

---

## 🗺️ 4. Recommended Execution Plan

Sprint **046** should pick up the low-risk batch — 18 rows (`S045-01, 03, 04, 05, 07, 08, 10, 11, 12, 13, 15, 16, 21, 23, 24, 25, 26, 28`): broken/stale-reference corrections (`install.sh`→`install.py`, `mass-standardizer`→`legacy_app_auditor.py`, bare `omni_minimizer.py`/`graph.json`/`graphify update` paths, the dead `#functional_lock` anchor, the missing `0_SYSTEM_ARCHITECTURE.md` end-state entry), the two `OBSOLETA` tombstone/pointer downgrades (`RA-04`, `RA-10`), the `RA-16` print-order swap, three docstring fixes (`check_gate_log.py`, `on_commit.py`, `audit_cursor_era.py`), the `state_mirror.py` bare-`except` fix, two `invoked_by:` token additions, two `vendored-reference` exception entries, the `frontend_modular_standard.md` `RA-06` retitle, and the `project_topology.md` nucleus-interpreter line — each is a one-file mechanical edit with a stated done-criterion.

A separate design pass (Sprint **047**, 10 rows — `S045-02, 06, 09, 14, 17, 18, 19, 20, 22, 27`) is needed where the fix is not mechanical: wiring a deterministic enforcer (or an explicit judgment-split declaration) for `§1 linter_command` + `Complexity`; rewriting `RA-01` into an executable directive with an invoker; the `RA-02` cross-file operative-text move under `RA-14`; a canonical SkillOpt `train_runner.py` invocation block referenced by three workflows; `RA-14`-bound parenthetical step-id passes over `reverse_documentation_workflow.md` and the `repository_hardening_workflow.md` table reshape; the `audit_workflow.md precision_audit` real-command design; and the two script enhancements (`verify_references.py` check (d) `#anchor` resolution, `map_workflows.py` non-step-row skip-marker), which are coupled.

One row is **deferred** (`S045-29`): adding `last_harden_run` to `docs/active_state.json` waits on a human policy call — whether the platform-hardening protocol applies to the nucleus itself or only to hosts. `S045-30` is a `RECORD` only (native git hooks correctly use `exit 1`; outside `RA-11` scope) — no action, logged so it is not re-raised.

**DO NOT TOUCH:** `agents.md §7` amendment IDs must **never** be renumbered. `RA-04` and `RA-10` receive **tombstone / pointer rows in place**, not deletion and not resequencing of `RA-05`..`RA-18` — there are 100+ external citations by number across sprints, hooks, guides and host records, and a renumber silently breaks every one of them.

---

## 🛠️ Three-File Skill Standard Verification

Not applicable — this synthesis unit produces one Markdown report and ships no `scripts/` folder.

- [n/a] **README.md**
- [n/a] **SKILL.md**
- [n/a] **scripts/**

---

## 🛡️ Certification

**Certified by the Rule Validator under Pipeline Methodology v4.27.0 (Sprint 045, Phase 6, Unit 4 — Wave 2 synthesis).**
Scope: consolidation of `NUCLEUS_RULESET_AUDIT_REPORT-045.md`, `NUCLEUS_WORKFLOW_AUDIT_REPORT-045.md`, `NUCLEUS_MECHANISM_AUDIT_REPORT-045.md`; cross-check against `docs/audits/UPSTREAM_FINDINGS_FROM_HOSTS.md`. Classification, ranking and drafted amendments only — **nothing applied** (`IMPLEMENTATION_PLAN.md` D1). No `agents.md`, `rules/*.md`, `workflows/*.md`, script or hook was modified.
*Deliverable lock: this is the single physical file authored by this subtask (`agents.md §2 jurisdictional_lock`).*
*Timestamp: 2026-09-07*
*Signature ID: A045-SYNTH-rule_validator*
