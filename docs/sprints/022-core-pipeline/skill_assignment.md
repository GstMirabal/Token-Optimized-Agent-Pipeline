# Skill Assignment — Sprint 022 (`model-tiering`)

## Used

| Skill / tool | Where |
| :--- | :--- |
| `claude-api` reference | **Decisive.** The tier table was about to be written from memory; loading the reference changed it materially — Fable 5 turned out to cost twice Opus, not to be a cheap top tier |
| `slash-commander`, `mass-standardizer`, `topology-monitor` | `make verify` |

## Not used, deliberately

| Skill | Why not |
| :--- | :--- |
| `token-saver-auditor` | Its `scripts/` holds only `__init__.py`. Sprint `030` work |
| `graphify` | Two new scripts and a config; a targeted read costs less |

## No skill was forged

Both new scripts are framework-internal and declare their invokers (`RA-16`).
