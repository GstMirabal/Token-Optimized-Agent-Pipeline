---
description: "Session-Close Protocol (Keyword: close)"
version: 5.0.0
---

# 🛡️ Workflow: Close (Atomic Liquidation V2)

Master closure protocol enforcing the Zero-Tolerance Amnesia rule and absolute Git state sovereignty.

## Execution Flow

| Phase | Step | Action / Constraint |
| :--- | :--- | :--- |
| **0. Constitutional Invocation** | `read_constitution` | Subagents MUST zero memory and read `agents.md`. Closure actions omitting explicit constitutional compliance are invalid. |
| **1. Heuristic Distillation** | `systemic_escalation` | If the sprint yielded systemic improvements, `Rule Validator` MUST formally index them into `agents.md` NOW. No massive interactive extractions are allowed. |
| **2. Zero-Tolerance Purge** | `memory_wipe` | Execute forced physical deletion (`rm`) of all temporary `.md` logs within the `/memory/` sprint directory. The directory MUST be emptied entirely. |
| **3. Roadmap Liquidation** | `state_sync` | Evaluate `docs/active_state.json`. If sprint is completely validated, switch state to `COMPLETED (100%)` and mirror to `.agent_state/mirror.json`. |
| **4. Git Sovereignty** | `atomic_commit` | Execute an atomic `git add .`, `git commit -m "chore: Sprint #02x closure"`, and `git push` on BOTH the main project repository and the `.agents` submodule sequentially. |
| **5. Golden Gate Lock** | `session_lock` | Output the official seal: **`SESSION LOCKED`**. The Matrix enters suspended animation. |

---
*Optimized for Matrix V2 Memory Sovereignty & Token Savings (v5.0.0).*
