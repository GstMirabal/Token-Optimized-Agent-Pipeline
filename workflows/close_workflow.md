---
description: "Session-Close Protocol (Keyword: close)"
version: 4.0.0
---

# 🛡️ Workflow: Close (Atomic Liquidation)

Master closure protocol enforcing Amnestic Extraction, state purge, and Matrix V2 operational sovereignty.

## 0. Constitutional Invocation (Phase 0 - Mandatory)
Before invoking transactional liquidation logic, the executing entity MUST zero its memory and read `agents.md` directly. Closure actions omitting explicit constitutional compliance are invalid.

## 1. Amnestic Extraction (Jurisprudence Loop)
The session's tactical knowledge MUST be distilled before its workspace memory is wiped.
- **Action:** Mandatory invocation of **`extract_workflow.md`** to perform domain-isolated extraction into the `/memory/` sub-directories.
- **Verification Handshake:** Under constitutional law, the agent MUST explicitly ask the user: *"Do you approve the indexing of these newly extracted Knowledge Items?"*
- **Rigorous Gate:** KIs CANNOT be inserted into any `memory_index.json` without Explicit User Approval.

## 2. Heuristic Learning (Systemic Distillation)
- **Action:** Ask the user: *"Do you want to perform telemetry distillation of this session to update the governance knowledge base?"*
- **Rule Validator Escalation:** If telemetry distillation generates systemic operational adjustments, the data MUST be sent strictly to the **[Rule Validator](../agents/rule_validator.md)** for proposed constitutional updates. Autonomous systemic adjustments bypassing QA checks are BANNED.

## 3. Roadmap Liquidation
Strategic settlement of the current strategic phase.
- **Milestone Audit:** Evaluate `docs/active_state.json`. If the planned sprint is validated as fully executed after passing the Double-Gate review, switch its active state to `COMPLETED (100%)`.
- **Master Index Sync:** Reflect this logical completion natively in the active `task_scope.md`.

## 4. Black Box Synchronization
- **Nuclear Mirroring:** Synchronize the updated state of `docs/active_state.json` out to the root-bound `.agent_state/mirror.json` to prevent topological desync in the next initiation.
- **Git Sovereignty & Hook Verification:** Submodule staging must securely pass the automated Python hooks running under `hooks/on_commit.py` to prevent accidental credential or API leakage.

## 5. Golden Gate Lock
- **Action:** Output the official seal: **`SESSION LOCKED`**. The matrix enters standard suspended animation, waiting for the subsequent `start_workflow.md` wake cycle.

---
*Optimized for Matrix V2 Topology and Memory Sovereignty (v4.0.0).*
