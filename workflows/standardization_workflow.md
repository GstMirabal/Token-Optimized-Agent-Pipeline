---
description: "Standardized Hierarchy Alignment Protocol (SHAP)"
version: 3.0.0
---

# 🛡️ Workflow: Standardization (Alineación)

Governance protocol to enforce structural sovereignty and linguistic purity.

## Execution Flow

| Phase | Step | Action / Constraint |
| :--- | :--- | :--- |
| **0. Trigger** | `condition_check` | Missing `/docs/` tree, unstructured naming, or linguistic drift (Spanish in code/docs). |
| **1. Scaffolding** | `matrix_mapper` | Instantiates `[Stack]/[Layer]/[Sprint_ID]/` hierarchy per Rule `mandatory_topology`. |
| **2. Purification**| `doc_orchestrator` | Converts Spanish text to Technical English. Converts lists to Markdown Tables. |
| **3. Topology** | `json_update` | Updates the `topology_map` inside `docs/active_state.json`. NO Mermaid/ASCII. |
| **4. Legacy** | `onboarding` | If mature project, executes Full Reverse Engineering to map into Matrix V2 hierarchy. |

---
*Optimized for Matrix V2 Hierarchy Alignment & Tabular Density (v3.0.0).*
