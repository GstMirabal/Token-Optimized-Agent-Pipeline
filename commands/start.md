---
description: "Session-Start Protocol (Keyword: start)"
---

1. Run `python3 scripts/session_start.py` (or `make session-start`); read the briefing.
2. Hand off to the **Principal Agent** for pipeline Phase 1 (Planning). Full binding steps: `@workflows/start_workflow.md`.
