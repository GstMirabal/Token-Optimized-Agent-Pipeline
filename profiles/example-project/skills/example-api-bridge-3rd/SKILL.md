---
name: example-api-bridge
description: Illustrative vendored (-3rd suffix) skill wrapping a fictional external API. Template for how a real project-family profile would bridge a real third-party data source.
---

# 🛠️ Local Skill: Example API Bridge (illustrative)

## Domain
- **Category:** Example
- **Origin:** Fictional — template only, not a real vendored skill.

## Technical Logic
Illustrates the shape a `-3rd`-suffixed, project-family skill takes: a thin wrapper script around a fictional external API, kept inside `profiles/[name]/skills/` rather than the framework's core `skills/` (`agents.md §3 topological_order`).
