# .agents / Memory Hub

Dynamic directory for local storage of volatile context (The Jurisprudence Loop).
Every transitive knowledge item created here strictly follows the **Scope Strict & Amnesia** mandate dictated by the Matrix Constitution (`agents.md`):

1. **Ephemeral Validation:** Resolved anomalies and session insights generate Markdown files that act as "Temporary Jurisprudence".
2. **Governance Escalation:** Every documented file waits for the *Governance Learner* to extract systemic patterns and inject them as global technical rules into `agents.md`.
3. **Isolated Namespaces:** Read/write operations must always be restricted to the isolated active namespace (e.g., `/core/` or `/cryptobot/`).
4. **Context Proxy:** The agent is strictly PROHIBITED from recursively listing subdirectories natively. Routing must be limited exclusively to parsing and interpreting the hyper-condensed (1-line) descriptors from the localized `memory_index.json` files.
